{#
/**
 * @file slider_image_entity.html.twig
 * Default theme implementation to present Slider image entity data.
 *
 * This template is used when viewing Slider image entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_slider_image_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('slider_image_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
